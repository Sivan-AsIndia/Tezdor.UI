import { ResponsiveTable } from './responsive-table';

describe('ResponsiveTable', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveTable();
    expect(directive).toBeTruthy();
  });
});
